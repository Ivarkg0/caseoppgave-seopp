package com.example.demo;

import org.springframework.stereotype.Service;

@Service
public class BrevService {
    public void sendAvtale(KundeDTO kunde, AvtaleDTO avtale) {
    //Try catch send avtale til kunde
    //if catch throw new RuntimeException("Feil ved sending av avtale til kunde");
    // lag en egen exception klasse

    }
}
